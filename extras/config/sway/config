#
# ~/.config/sway/config
#
# vim: et ts=4 sw=4

### TABLE OF CONTENTS
#   * Variables
#   * Autostart
#   * Output configuration
#     * Themeing
#   * Input configuration
#   * Key bindings
#     * Basics
#     * Movement
#     * Workspace bindings
#       * Workspace configs
#     * Layout bindings
#     * Application bindings
#   * Resizing
#   * Utilities
#   * Status bar

#
### WIP
#

#
### Variables
#
    # modifier keys
    set $mod Mod4
    set $alt Mod1
    set $ctl Ctrl
    set $shf Shift
    set $float space
    # direction keys
    set $left  h
    set $down  t
    set $up    n
    set $right s
    # preferred executables
    set $term foot
    set $menu exec wmenu-run -i -l 15 -f '$font' -p Run: -M ff8cda -m 000000 -N 8cdaff -n 000000 -S ff8cda -s 000000

include /etc/sway/config-vars.d/*

#
### Autostart
#
    # redshift
    # TODO: 3500 desktop, 3000 laptop
    exec wlsunset -t 3000 -T 5000 -s 21:00 -S 9:00
    # notification server
    # TODO: sync configs for swaync
    exec swaync
    # video wallpaper
    set $video_wallpaper ~/Downloads/fish-aquarium.1920x1080.mp4
    # this is stupid... but it works!
    # TODO: sway allows nested variables. use them
    #exec bash -c "pkill mpvpaper ; mpvpaper HDMI-A-1 --fork --mpv-options 'loop loop-playlist' $video_wallpaper ; mpvpaper HDMI-A-2 --fork --mpv-options 'loop loop-playlist' $video_wallpaper ; sleep 0.1 ; swaymsg reload"
    bindsym $mod+$shf+d nop exec bash -c "pkill mpvpaper ; mpvpaper HDMI-A-1 --fork --mpv-options 'loop loop-playlist' $video_wallpaper ; mpvpaper HDMI-A-2 --fork --mpv-options 'loop loop-playlist' $video_wallpaper ; sleep 0.1 ; swaymsg reload"

    # idle configuration aka powersaving aka screenlock
    # turn off monitors after 10 minutes, wait 30 seconds to lock, wait 5 minutes to sleep
    # old swayidle color: 354045
    exec swayidle -w \
        timeout 600 'swaymsg "output * power off"' \
        timeout 630 'swaylock -f -i /home/alex/Pictures/wallpapers/fork_yuri_wp7124847_blur.png' \
        timeout 900 'systemctl suspend' \
        resume 'swaymsg "output * power on"' \
        before-sleep 'swaylock -f -i /home/alex/Pictures/wallpapers/fork_yuri_wp7124847_blur.png' \
        ;

#
### Output configuration
#
    # configure output arrangement
    output eDP-1 resolution 1920x1080 position 0,0

    # set wallpapers
    # we also set a video wallpaper, but this is a fallback
    output * bg /home/alex/Pictures/wallpapers/fork_yuri_wp7124847.png fill

    # font
    set $font FiraCode Nerd Font 16
    font pango:$font

    # colors
    #       .class               outline   bckgrnd   frgrnd    indctr    border
      client.focused             #e667be   #ff8cda   #000000   #00ff00   #ff8cda
      client.focused_inactive    #888888   #5f676a   #ffffff   #484e50   #5f676a
      client.focused_tab_title   #888888   #5f676a   #ffffff   #484e50   #5f676a
      client.unfocused           #888888   #222222   #888888   #292d2e   #222222
    # client.urgent              #2f343a   #900000   #ffffff   #900000   #900000

    # borders
    set $borders normal 3
    default_border $borders
    #exec_always swaymsg [tiling] border $borders
    #exec_always swaymsg [floating] border $borders

    # configure gaps
    set $inner 10
    set $outer 20
    smart_gaps inverse_outer
    gaps inner $inner
    gaps outer $outer
    bindsym $mod+$ctl+g      gaps inner all set $inner ; gaps outer all set $outer
    bindsym $mod+$ctl+$shf+g gaps inner all set 0      ; gaps outer all set 0

    # miscellaneous options
    title_align center
    titlebar_padding 30 4
    force_display_urgency_hint 1000
    hide_edge_borders smart_no_gaps
    # kind of a training wheel until i learn J/K movement
    focus_wrapping no

#
### Input configuration
#
    # run 'swaymsg -t get_inputs' and 'man 5 sway-input'
    input type:keyboard {
        xkb_layout us
        xkb_variant dvorak
        xkb_options caps:ctrl_modifier,caps:swapcaps,ctrl:swap_lwin_lctl,ctrl:swap_rwin_rctl
    }

    #seat seat0 xcursor_theme breeze_cursors
    seat seat0 xcursor_theme catppuccin-latte-pink-cursors 36

    input type:pointer {
        accel_profile adaptive
        pointer_accel -0.35
    }

    input type:touchpad {
        tap enabled
        scroll_method two_finger
    }

    # hide cursor when idle, in ms
    seat * hide_cursor 5000

#
### Key bindings
#
    # Start a terminal
    bindsym $mod+Return exec $term
    bindsym $mod+$shf+Return exec $term -T - sleep infinity
    bindsym $mod+$alt+Return exec $term -T - watch -tw -n60 'echo ${COLUMNS}x${LINES} ; echo ${COLUMNS}x${LINES} | figlet -k'


    # Kill focused window
    bindsym $mod+$shf+q kill

    # Start your launcher
    bindsym $alt+Space $menu
    bindsym $mod+d exec $menu

    # Reload the configuration file
    bindsym $mod+$shf+c reload

    # Floating windows
    floating_modifier $mod normal
    bindsym $mod+$shf+$float floating toggle
    bindsym $mod+$float focus mode_toggle
    bindsym $mod+$alt+$float nop sticky toggle

    # Exit sway (logs you out of your Wayland session)
    bindsym $mod+$shf+e exec swaynag -t warning \
        -m 'You pressed the exit shortcut. Do you really want to exit sway? This will end your Wayland session.' \
        -B 'Yes, exit sway' 'swaymsg exit'


#
# Moving around:
#
    bindsym $mod+$left focus left
    bindsym $mod+$down focus down
    bindsym $mod+$up focus up
    bindsym $mod+$right focus right
    bindsym $mod+Left focus left
    bindsym $mod+Down focus down
    bindsym $mod+Up focus up
    bindsym $mod+Right focus right

    bindsym $mod+$shf+$left move left
    bindsym $mod+$shf+$down move down
    bindsym $mod+$shf+$up move up
    bindsym $mod+$shf+$right move right
    bindsym $mod+$shf+Left move left
    bindsym $mod+$shf+Down move down
    bindsym $mod+$shf+Up move up
    bindsym $mod+$shf+Right move right

#
# Workspaces:
#
    # workspace options
    workspace_auto_back_and_forth yes

    # set names
    set $ws1 10: main
    set $ws2 20: alt
    set $ws3 30: code
    set $ws4 40: game
    set $ws5 50: media
    set $ws6 60
    set $ws7 70
    set $ws8 80
    set $ws9 90: bg
    set $ws0 99

    # move by number
    bindsym $mod+1 workspace number $ws1
    bindsym $mod+2 workspace number $ws2
    bindsym $mod+3 workspace number $ws3
    bindsym $mod+4 workspace number $ws4
    bindsym $mod+5 workspace number $ws5
    bindsym $mod+6 workspace number $ws6
    bindsym $mod+7 workspace number $ws7
    bindsym $mod+8 workspace number $ws8
    bindsym $mod+9 workspace number $ws9
    bindsym $mod+0 workspace number $ws0
    bindsym $mod+$shf+1 move container to workspace number $ws1
    bindsym $mod+$shf+2 move container to workspace number $ws2
    bindsym $mod+$shf+3 move container to workspace number $ws3
    bindsym $mod+$shf+4 move container to workspace number $ws4
    bindsym $mod+$shf+5 move container to workspace number $ws5
    bindsym $mod+$shf+6 move container to workspace number $ws6
    bindsym $mod+$shf+7 move container to workspace number $ws7
    bindsym $mod+$shf+8 move container to workspace number $ws8
    bindsym $mod+$shf+9 move container to workspace number $ws9
    bindsym $mod+$shf+0 move container to workspace number $ws0

    # move between workspaces and outputs
    bindsym $mod+bracketleft workspace prev_on_output
    bindsym $mod+bracketright workspace next_on_output
    bindsym $mod+BackSpace workspace back_and_forth
    bindsym $mod+$shf+BackSpace move container to workspace back_and_forth
    bindsym $alt+Tab focus output right
    #bindsym $mod+slash focus output left
    #bindsym $mod+equal focus output right

    # scratchpad (technically kind of a workspace)
    bindsym $mod+$shf+minus move scratchpad
    bindsym $mod+minus scratchpad show

#
# Layout stuff:
#
    # left-hand layout keys
    bindsym $mod+o layout toggle tabbed stacking
    bindsym $mod+comma layout split tabbed stacking
    bindsym $mod+period layout toggle split

    # right-hand layout keys
    bindsym $mod+c layout toggle split
    bindsym $mod+r layout toggle tabbed stacking
    bindsym $mod+l split none
    bindsym $mod+w splith
    bindsym $mod+v splitv
    bindsym $mod+z workspace back_and_forth

    # fullscreen
    bindsym $mod+f fullscreen
    bindsym $mod+u fullscreen

    # parent/child
    bindsym $mod+a focus parent
    bindsym $mod+$shf+a focus child
    bindsym $mod+g focus parent
    bindsym $mod+$shf+g focus child

#
# Application binds
#
    bindsym $mod+$alt+f exec env MOZ_ENABLE_WAYLAND=1 firefox
    bindsym $mod+$alt+$shf+f exec env MOZ_ENABLE_WAYLAND=1 firefox --private-window
    bindsym $mod+$alt+b exec blueman-manager
    bindsym $mod+$alt+g exec gimp
    bindsym $mod+$alt+v exec pavucontrol-qt
    bindsym $mod+$alt+$shf+v exec foot pulsemixer

#
# Resizing containers:
#
    bindsym $mod+$shf+r mode "resize"
    mode "resize" {
        bindsym $left resize shrink width 10px
        bindsym $down resize grow height 10px
        bindsym $up resize shrink height 10px
        bindsym $right resize grow width 10px
        bindsym Left resize shrink width 10px
        bindsym Down resize grow height 10px
        bindsym Up resize shrink height 10px
        bindsym Right resize grow width 10px

        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+$shf+r mode "default"
        bindsym $mod+r mode "default"
    }

#
# Utilities:
#
    # Special keys to adjust volume via PulseAudio
    bindsym --locked XF86AudioMute exec pactl set-sink-mute \@DEFAULT_SINK@ toggle
    bindsym --locked XF86AudioLowerVolume exec pactl set-sink-volume \@DEFAULT_SINK@ -5%
    bindsym --locked XF86AudioRaiseVolume exec pactl set-sink-volume \@DEFAULT_SINK@ +5%
    bindsym --locked XF86AudioMicMute exec pactl set-source-mute \@DEFAULT_SOURCE@ toggle

    # Special keys to adjust brightness via brightnessctl
    bindsym --locked XF86MonBrightnessDown exec brightnessctl set 5%-
    bindsym --locked XF86MonBrightnessUp exec brightnessctl set 5%+

    # Take screenshots
    set $screenshot_path ~/Pictures/Screenshot_$(date +"%F_%H%M%S.png")
    set $focused_output "$(swaymsg -t get_outputs | jq '.[] | select(.focused).name' -r)" 
    # this is way too many bindings but i'm just covering my bases because i'm kinda dumb
    bindsym --release Print           exec grim -g "$(slurp -d)"   $screenshot_path
    bindsym --release $shf+Print      exec grim -g "$(slurp -d)"   $screenshot_path
    bindsym --release $ctl+Print      exec grim -o $focused_output $screenshot_path
    bindsym --release $mod+Print      exec grim -g "$(slurp -d)"   $screenshot_path
    bindsym --release $mod+$shf+Print exec grim -g "$(slurp -d)"   $screenshot_path
    bindsym --release $mod+$ctl+Print exec grim -o $focused_output $screenshot_path
    bindsym --release $mod+p          exec grim -g "$(slurp -d)"   $screenshot_path
    bindsym --release $mod+$shf+p     exec grim -o $focused_output $screenshot_path

#
# Status Bar:
#
    bar {
        tray_output none
        font pango:$font
        position top
        status_command i3blocks
        separator_symbol " | "
        colors {
            background #323232
            statusline #ffffff
            separator #70afcc
            #                  border  bckgrn  text
            focused_workspace  #e667be #ff8cda #000000
            inactive_workspace #888888 #323232 #ffffff
        }
    }

include /etc/sway/config.d/*
